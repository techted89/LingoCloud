<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Required on Android 11+ to list installed apps in the whitelist UI
         This app is an Xposed module designed to hook into arbitrary apps on the system, thus requiring package visibility. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <permission android:name="com.LingoCloudTranslate.lingocloud.permission.IPC" android:protectionLevel="signature" />
    <uses-permission android:name="com.LingoCloudTranslate.lingocloud.permission.IPC" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LingoCloud"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher">

        <!-- Xposed Module Metadata -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <meta-data
            android:name="xposeddescription"
            android:value="Real-time UI translation via Gemini &amp; Microsoft APIs. Target: SDK 35." />

        <meta-data
            android:name="xposedminversion"
            android:value="93" />

        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />

        <!-- Settings Activity (Module Control Center) -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AppSelectionActivity"
            android:exported="false"
            android:label="@string/app_whitelist_title" />

        <!-- Translation Foreground Service -->
        <service
            android:name=".TranslationServer"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            android:enabled="true" />

        <!-- High-Speed IPC ContentProvider -->
        <provider
            android:name=".TranslationProvider"
            android:authorities="com.LingoCloudTranslate.lingocloud.provider"
            android:exported="true"
            android:permission="com.LingoCloudTranslate.lingocloud.permission.IPC" />

    </application>
</manifest>
